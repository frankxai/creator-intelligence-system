/**
 * @cis/core — Creator Intelligence Protocol (CIP) types and validators.
 *
 * This is the protocol surface. Every CIS adapter, every conductor,
 * every implementation in any language conforms to these shapes.
 *
 * See SPEC.md in the repo root for the full protocol spec.
 */

export * from './types.js'
